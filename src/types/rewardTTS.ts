export type rewardTTS = {
    userId: string,
    rewardId: string,
    redemptionDate: Date,
    pointCost: number,    
    type: string,
    value: string,
    voice: number, 
    pitch: number,
    rate: number, 
    volume: number
}